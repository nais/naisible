{% for item in query('inventory_hostnames', 'workers:!storage_nodes:!ceph_nodes') %}
- host: "{{ lookup('dig', item) }}"
  port: {{ bigip_port | default(443) }}
  partition: "{{ bigip_partition }}"
{% endfor %}
