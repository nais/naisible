apiVersion: config.istio.io/v1alpha2
kind: EgressRule
metadata:
  name: ${domain}-egress-rule
spec:
  destination:
    service: "*.${domain}"
  ports:
    - port: 443
      protocol: https
    - port: 8443
      protocol: https
    - port: 9443
      protocol: https
