---
# Basic cluster health
- hosts: masters
  user: deployer
  become: yes
  tasks:
 
    - name: Get component status
      shell: kubectl get cs
      register: compnent_status

    - name: Check component status
      
